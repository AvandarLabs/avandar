/**
 * Values aligned with `seed/SeedData.ts` so global setup and specs stay free of
 * `@models` imports (Playwright cannot load those graphs cleanly).
 */
export const E2E_PRIMARY_USER_EMAIL = "user@avandarlabs.com";
export const E2E_PRIMARY_USER_PASSWORD = "avandar";
export const E2E_SEEDED_WORKSPACE_SLUG = "avandar-labs";
