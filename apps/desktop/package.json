{
  "name": "@avandar/desktop",
  "version": "0.0.0",
  "description": "Avandar desktop shell (Electrobun)",
  "license": "CPAL-1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "electrobun dev",
    "build": "electrobun build",
    "test": "vitest run -c vitest.config.ts",
    "test:integration": "find main -name '*.integration.test.ts' -print0 | xargs -0 bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "catalog:",
    "duckdb": "^1.4.4",
    "electrobun": "latest"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "bun": ">=1.1.0",
    "node": ">=22.0.0"
  }
}
