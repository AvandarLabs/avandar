import { defineRoutes, GET } from "@sbfn/_shared/MiniServer/MiniServer.ts";
import type { $FUNCTION_NAME_PASCAL_CASE$API } from "@sbfn/$FUNCTION_NAME$/$FUNCTION_NAME$.routes.types.ts";

/**
 * This is the route handler for all $FUNCTION_NAME$ endpoints.
 */
export const Routes = defineRoutes<$FUNCTION_NAME_PASCAL_CASE$API>(
  "$FUNCTION_NAME$",
  {
    "/": {
      GET: GET("/").action(() => {
        return { success: true };
      }),
    },
  },
);
