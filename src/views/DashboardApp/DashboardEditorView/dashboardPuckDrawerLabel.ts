/**
 * Puck drawer labels for components/categories. Dashboard puck strings are not
 * in compiled Lingui catalogs yet; `t` would render empty in the sidebar.
 */
export function puckDrawerLabel(english: string): string {
  return english;
}
